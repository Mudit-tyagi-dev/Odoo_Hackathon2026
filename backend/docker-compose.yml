services:
  postgresql:
    image: postgres:17-alpine
    container_name: postgres-odoo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  postgres_data: